name: the_app
description: Tinh tế mobile apps built with Flutter for evaluation purposes.
version: 2.0.0+27
publish_to: none

environment:
  flutter: 2.0.1
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  # firebase
  firebase_core: 1.0.0
  firebase_messaging: ^9.0.0
  firebase_crashlytics: ^1.0.0

  # ours
  flutter_widget_from_html: ^0.6.0-0
  fwfh_webview: any
  the_api:
    path: ./packages/api
  
  apple_sign_in: ^0.1.0
  convert: ^2.1.1
  crypto: any
  flutter:
    sdk: flutter
  flutter_auth_buttons: ^0.8.0
  flutter_cache_manager: any
  flutter_custom_tabs: ^0.6.0
  flutter_facebook_login: ^3.0.0
  flutter_localizations:
    sdk: flutter
  flutter_speed_dial: 1.2.5
  font_awesome_flutter: ^8.8.1
  google_sign_in: ^4.5.1
  html_unescape: ^1.0.1+3
  http: any
  image_picker: ^0.7.0
  intl: any
  intl_translation: any
  package_info: ^0.4.1
  photo_view: ^0.9.2
  provider: ^4.1.3
  share: ^0.6.4+3
  shared_preferences: ^0.5.7+3
  scroll_to_index: ^1.0.3
  timeago: ^2.0.26

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  intl: ^0.17.0
  petitparser: ^4.0.0
  url_launcher: ^6.0.0
  xml: ^5.0.0

flutter:
  assets:
    - assets/
  uses-material-design: true
