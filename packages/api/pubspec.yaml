name: the_api

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  async:
  flutter:
    sdk: flutter
  freezed_annotation:
  http:
  json_annotation:
  path:

dev_dependencies:
  build_runner: ^2.0.4
  flutter_lints: ^1.0.3
  freezed: ^0.14.2
  json_serializable: ^6.0.0
  test: ^1.17.5
