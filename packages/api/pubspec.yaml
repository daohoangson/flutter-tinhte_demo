name: the_api

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  async:
  freezed_annotation:
  http:
  json_annotation:
  path:

dev_dependencies:
  build_runner:
  freezed:
  json_serializable:
  test:

dependency_overrides:
  http_multi_server: ^3.0.0
  io: ^1.0.0
  shelf_web_socket: ^1.0.0
  web_socket_channel: ^2.0.0
