name: the_api

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  async:
  convert: ^3.0.0
  crypto: ^3.0.0
  flutter:
    sdk: flutter
  freezed_annotation: ^2.1.0
  http: ^0.13.5
  json_annotation: ^4.8.1
  path:

dev_dependencies:
  build_runner: ^2.0.4
  flutter_lints: ^2.0.0
  freezed: ^2.1.0
  json_serializable: ^6.0.0
  test: ^1.17.5
